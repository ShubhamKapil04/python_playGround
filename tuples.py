coordinates = (4, 5)
# Can't modify tuple
# coordinates[1] = 10
# So we can't change the data
print(coordinates[1])

# We also can create the tuple of list

coordinate = [(4, 5), (6, 10), (40, 54), (54, 90)]
print(coordinate)
